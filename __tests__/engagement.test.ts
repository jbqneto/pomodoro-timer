import { describe, expect, it } from 'vitest';
import { getReturnGap, shouldPromptUsefulnessFeedback } from '@/application/engagement/engagement-rules';
import { LocalStorageEngagementRepository, ENGAGEMENT_STORAGE_KEY } from '@/infrastructure/persistence/local-storage-engagement.repository';
describe('engagement rules',()=>{it('buckets local calendar returns',()=>{expect(getReturnGap('2026-01-01','2026-01-02')).toBe('next_day');expect(getReturnGap('2026-01-01','2026-01-07')).toBe('2_to_7_days');expect(getReturnGap('2026-01-01','2026-01-09')).toBe('8_plus_days')});it('uses a fourteen-day prompt cadence',()=>{expect(shouldPromptUsefulnessFeedback({version:1,lastFocusCompletionDate:'2026-01-01',lastFeedbackPromptDate:'2026-01-02'},'2026-01-15')).toBe(false);expect(shouldPromptUsefulnessFeedback({version:1,lastFocusCompletionDate:'2026-01-01',lastFeedbackPromptDate:'2026-01-02'},'2026-01-16')).toBe(true)});it('recovers from corrupt persistence',()=>{localStorage.setItem(ENGAGEMENT_STORAGE_KEY,'bad');expect(new LocalStorageEngagementRepository(localStorage).load()).toEqual({version:1,lastFocusCompletionDate:null,lastFeedbackPromptDate:null})})});
